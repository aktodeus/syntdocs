services:
  - type: web
    name: syntdocs-nexus
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn nexus_server:app --host 0.0.0.0 --port $PORT
    plan: free
